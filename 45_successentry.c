#include <stdio.h>

int main()
{
   int n,range,i;
   scanf("%d %d",&n,&range);
       if(n>range)
       {
           printf("Error enter an valid input");
       }
       else
       {
   printf("Successfull entry of a number %d",n);
       }
        return 0;
}
