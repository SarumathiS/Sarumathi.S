#include <stdio.h>

int main()
{
     
      int n,ln,i;
      scanf("%d",&n);
       char str[n];
      gets(str);
      ln=strlen(str)-1;
      for(i=0;i<ln;i++)
      {
          printf("\nHello");
      }

}
